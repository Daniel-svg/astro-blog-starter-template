---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

const title = 'Domain 查詢工具';
const description = '使用 Cloudflare DNS-over-HTTPS 查詢 A/AAAA/NS/MX 記錄與是否可用';
---

<!doctype html>
<html lang="zh-Hant">
  <head>
    <BaseHead title={title} description={description} />

    <style>
      body {
        background: var(--bg);
        margin: 0;
        font-family: Inter, system-ui, sans-serif;
      }

      main.container {
        width: min(100% - 2rem, 860px);
        margin: 40px auto;
      }
      .source {
         text-align: center !important;
      }
      .card {
        background: var(--panel);
        padding: 26px;
        border-radius: var(--card-radius);
        border: 1px solid var(--border);
        width: 100%;             /* ⭐ 確保永遠不超過螢幕 */
        box-sizing: border-box;  /* ⭐ padding 不會把卡片撐爆 */
      }


      h1 {
        margin: 0 0 14px;
        font-size: 1.6rem;
        font-weight: 700;
      }

      .card {
        background: white;
        border: 1px solid rgba(0,0,0,0.08);
        border-radius: 14px;
        padding: 24px;
        margin-top: 24px;
        box-shadow: 0 4px 10px rgba(0,0,0,0.05);
      }

      /* Cloudflare 風格輸入區 */
      .input-box {
        display: flex;
        gap: 10px;
        margin-bottom: 18px;
        width: 100%;
      }

      .input-box input {
        flex: 1;
        padding: 14px 16px;
        border: 1px solid var(--border);
        border-radius: 10px;
        font-size: 1rem;
        width: 100%;
        box-sizing: border-box;
      }

      .input-box input:focus {
        border-color: #555;
        outline: none;
        box-shadow: 0 0 0 2px rgba(37,99,235,0.15);
      }

      button {
        background: #1e293b;
        border: none;
        color: #fff;
        padding: 0 20px;
        border-radius: 10px;
        cursor: pointer;
        font-weight: 600;
        white-space: nowrap;
      }

      button:hover {
        background: #0f172a;
      }

     @media (max-width: 780px) {
      .card {
         padding: 16px;  /* ⭐ 手機更舒服 */
      }
    }

      .source {
         text-align: center;
      }

      .result-box {
        margin-top: 20px;
        padding: 20px;
        border-radius: 12px;
        border: 1px solid var(--border);
        background: #f8fafc;
      }

      .row {
        padding: 10px 0;
        display: flex;
        justify-content: space-between;
        border-bottom: 1px solid rgba(0,0,0,0.05);
      }

      .row:last-child {
        border-bottom: none;
      }

      .label {
        color: var(--muted);
      }
      .grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
        gap: 16px;
      }
      .title {
        font-size: 1.6rem;
        font-weight: bold;
        margin-bottom: 16px;
      }
    </style>
  </head>

  <body>
    <Header />
    <main class="container">
      <div class="card">
        <h1>Domain 查詢工具</h1>
        <div class="desc">使用 Cloudflare DNS-over-HTTPS 查詢 A / AAAA / NS / MX 記錄，並判斷是否可註冊。</div>

        <div class="input-box">
          <input id="domainInput" placeholder="輸入 Domain，例如：example.com" />
          <button id="searchBtn">查詢</button>
        </div>
      </div>

      <div class="card">
        <div class="title">查詢結果</div>
        <div class="grid" id="resultArea">
          <!-- 動態填入 -->
        </div>
      </div>
      <p class="source">資料來源：Cloudflare DNS-over-HTTPS</p>
    </main>
    <Footer />

<script>
  const input = document.getElementById("domainInput");
  const btn = document.getElementById("searchBtn");
  const area = document.getElementById("resultArea");

  btn.addEventListener("click", search);
  input.addEventListener("keydown", (e) => {
    if (e.key === "Enter") search();
  });

  // 查詢 Cloudflare DoH
  async function queryDNS(domain, type) {
    const url = `https://cloudflare-dns.com/dns-query?name=${domain}&type=${type}`;
    const res = await fetch(url, {
      headers: { "accept": "application/dns-json" }
    });
    return res.json();
  }

  function parseAnswer(ans) {
    if (!ans || !ans.Answer) return null;
    return ans.Answer.map(r => `${r.data} <span style="color:#64748b;">(TTL ${r.TTL})</span>`).join("<br>");
  }

  async function search() {
    const domain = input.value.trim();
    if (!domain) return;

    area.innerHTML = `<div class="result-box">查詢中...</div>`;

    try {
      const [A, AAAA, CNAME, TXT, MX, NS, SOA, DNSKEY] = await Promise.all([
        queryDNS(domain, "A"),
        queryDNS(domain, "AAAA"),
        queryDNS(domain, "CNAME"),
        queryDNS(domain, "TXT"),
        queryDNS(domain, "MX"),
        queryDNS(domain, "NS"),
        queryDNS(domain, "SOA"),
        queryDNS(domain, "DNSKEY")
      ]);

      const result = {
        A: parseAnswer(A),
        AAAA: parseAnswer(AAAA),
        CNAME: parseAnswer(CNAME),
        TXT: TXT.Answer ? TXT.Answer.map(r => r.data.replace(/"/g, "")).join("<br>") : null,
        MX: MX.Answer ? MX.Answer.map(r => r.data).join("<br>") : null,
        NS: parseAnswer(NS),
        SOA: SOA.Answer ? SOA.Answer.map(r => r.data).join("<br>") : null,
        DNSSEC: DNSKEY.Answer ? "🟢 啟用" : "🔴 未啟用",
        available: A.Status === 3 && AAAA.Status === 3 && MX.Status === 3 // NXDOMAIN
      };

      area.innerHTML = `
        <div class="result-box">

          <div class="row">
            <span class="label">Domain</span><span>${domain}</span>
          </div>

          <div class="row">
            <span class="label">可用？</span>
            <span>${result.available ? '✅ 可註冊' : '❌ 已被使用'}</span>
          </div>

          <div class="row"><span class="label">A</span><span>${result.A || '—'}</span></div>
          <div class="row"><span class="label">AAAA</span><span>${result.AAAA || '—'}</span></div>
          <div class="row"><span class="label">CNAME</span><span>${result.CNAME || '—'}</span></div>
          <div class="row"><span class="label">MX</span><span>${result.MX || '—'}</span></div>
          <div class="row"><span class="label">NS</span><span>${result.NS || '—'}</span></div>

          <div class="row"><span class="label">TXT</span><span>${result.TXT || '—'}</span></div>

          <div class="row"><span class="label">SOA</span>
            <span style="text-align:right">${result.SOA || '—'}</span>
          </div>

          <div class="row"><span class="label">DNSSEC</span><span>${result.DNSSEC}</span></div>

        </div>
      `;
    } catch (err) {
      area.innerHTML = `<div class="result-box">❌ 查詢失敗（請稍後再試）</div>`;
    }
  }
</script>


  </body>
</html>
