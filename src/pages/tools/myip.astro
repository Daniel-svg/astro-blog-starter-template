---
import BaseHead from '../../components/BaseHead.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../../consts';

const title = '我的 IP 查詢工具';
const description = '快速查詢您的公開 IP 地址及地理位置資訊。';
---

<!doctype html>
<html lang="zh-Hant">
  <head>
    <BaseHead title={title} description={description} />

    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css"
      integrity="sha512-1y6w3p0G6g7G1xL2nC9n8FzVj9hK4k5L5gD5j6g7O2xS4i5xP7xT7p8D2o4b4g7C6g5e7M5w6X9G3gH4m1o5Q=="
      crossorigin="anonymous"
      referrerpolicy="no-referrer"
    />

    <script
      async
      src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-6026639928482840"
      crossorigin="anonymous"
    ></script>

    <style>
		/* 確保全域不會橫向溢出 */
		html, body {
			overflow-x: hidden;
			background-color: #f7f7f7; /* 設置背景色讓卡片更突出 */
		}
		
		.ip-main-content {
			max-width: 760px;
			margin: 0 auto;
			padding: 2.5rem 1rem;
		}

		.card {
			padding: 3rem 2rem;
			border-radius: 16px;
			background: #ffffff;
			box-shadow: 0 6px 25px rgba(0, 0, 0, 0.08);
			text-align: center;
		}

		.card h1 {
			font-size: 1.9rem;
			font-weight: 600;
			color: #374151;
			display: flex;
			align-items: center;
			justify-content: center;
			gap: 8px;
		}

		.ip-address {
			margin: 1.8rem auto;
			padding: 1rem 1.6rem;
			background: #eef4ff;
			color: #2563eb;
			border-radius: 12px;
			font-size: 2.4rem;
			font-weight: 700;
			font-family: "Roboto Mono", monospace;
			display: inline-block;
			/* 確保長 IP 在手機上可以換行 */
			word-break: break-all; 
		}

		/* 資訊兩欄 Grid 佈局 */
		.details {
			display: grid;
			grid-template-columns: 1fr 1fr; /* 桌面版兩欄 */
			gap: 14px 30px;
			margin: 2rem auto 2rem auto;
			max-width: 600px;
			text-align: left;
		}

		.detail-box {
			background: #f9fafb;
			padding: 12px 15px;
			border-radius: 10px;
			font-size: 1.05rem;
			color: #374151;
			border-left: 4px solid #3b82f6;
			display: flex;
			align-items: center;
			gap: 10px;
			/* 確保文字在 box 內能自動換行 */
			word-break: break-word; 
		}

		.detail-box i {
			color: #3b82f6;
			flex-shrink: 0;
			font-size: 1rem;
		}
		
		/* ----------------------------- */
		/* 行動版優化 (小於 640px) */
		@media (max-width: 640px) {
			.details {
				grid-template-columns: 1fr; /* 行動版變回單欄 */
				gap: 14px 0; /* 移除左右間距 */
				padding: 0 10px; /* 增加兩側內邊距，防止貼邊 */
			}
			.ip-main-content {
				padding: 1.5rem 0.5rem; /* 縮小主內容區 padding */
			}
			.card {
				padding: 2rem 1rem; /* 縮小卡片 padding */
			}
			.ip-address {
				font-size: 1.8rem; /* 縮小 IP 字體 */
			}
			.detail-box {
				font-size: 1rem; /* 調整細節資訊字體 */
			}
		}
		/* ----------------------------- */

		/* 按鈕 */
		.card button {
			margin-top: 10px;
			padding: 12px 26px;
			background: #3b82f6;
			color: white;
			border: none;
			border-radius: 10px;
			font-size: 1.1rem;
			cursor: pointer;
			transition: 0.25s;
		}

		.card button:hover {
			background: #1d4ed8;
			box-shadow: 0 4px 10px rgba(0, 0, 0, 0.15);
		}

		/* 資料來源 */
		.data-source {
			margin-top: 2.8rem;
			padding-top: 1.3rem;
			border-top: 1px solid #e5e7eb;
			font-size: 0.88rem;
			color: #6b7280;
		}
    </style>
  </head>

  <body>
    <Header />

    <main class="ip-main-content">
      <div class="card">
        <h1>🌐 您的公開 IP 地址是</h1>
        <div class="ip-address" id="ip">載入中...</div>

        <div class="details" id="details">
          <div class="detail-box"><i class="fas fa-globe"></i>國家：載入中</div>
          <div class="detail-box"><i class="fas fa-city"></i>城市：載入中</div>
          <div class="detail-box"><i class="fas fa-map-marker-alt"></i>地區/州：載入中</div>
          <div class="detail-box"><i class="fas fa-building"></i>ISP：載入中</div>
          <div class="detail-box"><i class="fas fa-clock"></i>時區：載入中</div>
          <div class="detail-box"><i class="fas fa-map-pin"></i>經緯度：載入中</div>
        </div>

        <button onclick="fetchIP()">重新整理 / 再次查詢</button>

        <div class="data-source">
          <p>數據來源：</p>
          <p>
            公開 IP 查詢由
            <a href="https://www.ipify.org/" target="_blank" rel="noopener noreferrer"
              >ipify</a
            >
            提供；地理位置資訊由
            <a href="https://ipinfo.io/" target="_blank" rel="noopener noreferrer"
              >IPinfo</a
            >
            提供。
          </p>
        </div>
      </div>
    </main>

    <Footer />

    <script is:inline>
      async function fetchIP() {
        const ipElement = document.getElementById('ip');
        const detailsElement = document.getElementById('details');
        const apiToken = '1b172809b09566'; // IPinfo API Token

        // 1. 重置顯示狀態
        ipElement.textContent = '載入中...';
        detailsElement.innerHTML = `
          <div class="detail-box"><i class="fas fa-globe"></i>國家：載入中</div>
          <div class="detail-box"><i class="fas fa-city"></i>城市：載入中</div>
          <div class="detail-box"><i class="fas fa-map-marker-alt"></i>地區/州：載入中</div>
          <div class="detail-box"><i class="fas fa-building"></i>ISP：載入中</div>
          <div class="detail-box"><i class="fas fa-clock"></i>時區：載入中</div>
          <div class="detail-box"><i class="fas fa-map-pin"></i>經緯度：載入中</div>
        `;

        try {
          // 2. 查詢 IP
          const ipRes = await fetch('https://api.ipify.org?format=json');
          if (!ipRes.ok) throw new Error('無法從 ipify 獲取 IP');
          const ipData = await ipRes.json();
          const ip = ipData.ip;
          ipElement.textContent = ip;

          // 3. 查詢地理位置資訊
          const infoRes = await fetch(
            `https://ipinfo.io/${ip}/json?token=${apiToken}`
          );
          if (!infoRes.ok) throw new Error('無法從 IPinfo 獲取地理資訊');
          const infoData = await infoRes.json();

          // 處理經緯度 (loc 格式通常為 "lat,lon")
          const latLonDisplay = infoData.loc ? infoData.loc.replace(',', ', ') : '未知';
          
          // 提取 ISP 名稱 (只取第一個單詞)
          const ispName = infoData.org ? infoData.org.split(' ').slice(1).join(' ') || infoData.org : '未知';

          // 4. 顯示結果
          detailsElement.innerHTML = `
            <div class="detail-box"><i class="fas fa-globe"></i>國家：${infoData.country || '未知'}</div>
            <div class="detail-box"><i class="fas fa-city"></i>城市：${infoData.city || '未知'}</div>
            <div class="detail-box"><i class="fas fa-map-marker-alt"></i>地區/州：${infoData.region || '未知'}</div>
            <div class="detail-box"><i class="fas fa-building"></i>ISP：${ispName}</div>
            <div class="detail-box"><i class="fas fa-clock"></i>時區：${infoData.timezone || '未知'}</div>
            <div class="detail-box"><i class="fas fa-map-pin"></i>經緯度：${latLonDisplay}</div>
          `;
        } catch (error) {
          console.error("IP 查詢錯誤:", error);
          ipElement.textContent = '無法取得 IP';
          detailsElement.innerHTML = `
            <div class="detail-box" style="grid-column: 1 / -1; border-left: 4px solid #dc2626;">
              <i class="fas fa-exclamation-triangle" style="color: #dc2626;"></i>載入失敗，請檢查網路或 API Token (錯誤: ${error.message})
            </div>
          `;
        }
      }

      // 頁面載入後立即執行查詢
      document.addEventListener('DOMContentLoaded', fetchIP);
    </script>
  </body>
</html>